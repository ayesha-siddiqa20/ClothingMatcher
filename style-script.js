// function changeStyle(style) {
//   if (style) {
//       // Update the style title
//       document.getElementById('style-title').textContent = style;
//       // Update the images for the new style
//       // This is where you would make an API call or update the images based on the selected style
//   }
// }

// // On page load, populate with the selected style
// document.addEventListener('DOMContentLoaded', function() {
//   const selectedStyle = localStorage.getItem('selectedStyle');
//   if (selectedStyle) {
//       document.getElementById('style-title').textContent = selectedStyle;
//       // Here you would also make an API call or update the images based on the stored style
//   }
// });
